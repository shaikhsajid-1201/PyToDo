CREATE TABLE IF NOT EXISTS todolist(id INT PRIMARY KEY,
                                    title VARCHAR(20),
                                    description VARCHAR(100)
                                    );

